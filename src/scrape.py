import requests
import re

def scrape_simplify():
    url: str
    response = requests.get(url)
    response.raise_for_status()
    content = response.text
    
def scrape_jobright():
    url: str
    response = requests.get(url)
    response.raise_for_status()
    content = response.text
    
    
    